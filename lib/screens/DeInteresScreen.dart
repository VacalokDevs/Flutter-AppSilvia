import 'package:flutter/material.dart';

class DeInteresScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('De Interés - Lugares turísticos'),
      ),
      body: Center(
        child: Text('Pantalla de De Interés'),
      ),
    );
  }
}
