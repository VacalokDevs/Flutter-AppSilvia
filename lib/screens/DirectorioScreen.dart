import 'package:flutter/material.dart';

class DirectorioScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Directorio - Comercios'),
      ),
      body: Center(
        child: Text('Pantalla de Directorio'),
      ),
    );
  }
}
