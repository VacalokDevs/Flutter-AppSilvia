import 'package:flutter/material.dart';

class EventosScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Eventos - Silvia Cauca'),
      ),
      body: Center(
        child: Text('Pantalla de Eventos'),
      ),
    );
  }
}
